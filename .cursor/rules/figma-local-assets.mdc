---
description: Figma MCP 资源须落盘本地或 CDN；禁止把 figma.com/api/mcp/asset 临时链长期留在代码里；落盘时按真实格式命名；各页面模块须推理补全设计稿中的 hover、交互态与操作后 toast
alwaysApply: false
---

# Figma MCP 资源与本地静态资源

## 临时链接（必须知晓）

1. **`https://www.figma.com/api/mcp/asset/...`** 为 Figma MCP 提供的**短期**资源地址（官方说明约 **7 天**有效），**不得**作为上线或可合并代码的长期方案。
2. 从 Figma 实现 UI、接入图片/图标时，**默认流程**应为：拉取 MCP 给出的 URL → **立即下载**到仓库内（如 `public/figma-assets/` 或 `src/assets/`）→ 代码中只引用 **本地路径**或你们自有 **CDN** URL。
3. 合并进主分支前，应检索仓库内是否仍存在 `figma.com/api/mcp/asset`，若有则替换为本地或 CDN。

## 落盘时的文件格式（避免裂图）

4. **禁止**在未校验内容的情况下，把所有下载文件统一命名为 `.png`（或其它单一扩展名）。
5. **落盘前先识别格式**：至少检查文件头魔数——`89 50 4E 47` 为 PNG；`3C` 开头且为文本时多为 **SVG**（`<svg` / `<?xml`）；`FF D8 FF` 为 JPEG；`GIF8` 为 GIF。或依据 HTTP `Content-Type`（`image/svg+xml`、`image/png` 等）选扩展名。
6. **代码中的引用路径**必须与磁盘扩展名一致（`.svg` 用 `.svg`，`.png` 用 `.png`），避免 `<img>`、`mask-image`、`url()` 因扩展名与 MIME 不匹配导致裂图或部分浏览器不显示。
7. 批量下载脚本里应「按响应类型/魔数写文件」，并在下载后抽样校验可读性（必要时用脚本检测魔数）。

## 业务数据

8. 列表、卡片、表格等任意模块若设计稿仅为占位，**静态数据**可写在组件内；接真实接口时，将数据源替换为接口返回结构即可，**图片 URL 仍应优先使用你们 CDN 或本地已落盘资源**，避免把 MCP asset 链塞进接口字段长期依赖。

## 推理与交互补全

9. 从设计稿实现**任意页面与业务模块**时，应**推理并补全**设计稿中已暗示但未单独画出的 **hover / focus / disabled / 加载中** 等状态与**可点区域的交互反馈**；对会产生结果或副作用的操作（提交、删除、导入、复制等），在用户操作后应提供 **toast 或其它明确反馈**（文案与时机与设计意图或产品约定一致）。
